<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- TextureView supports rotation transforms unlike SurfaceView -->
    <TextureView
        android:id="@+id/textureView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center" />

    <!-- Enrollment overlay - shown when device is not registered -->
    <LinearLayout
        android:id="@+id/enrollmentOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="#1a1a2e"
        android:padding="40dp"
        android:visibility="gone">

        <!-- Icon -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📱"
            android:textSize="80sp"
            android:layout_marginBottom="24dp" />

        <!-- Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Device Not Enrolled"
            android:textColor="#ffffff"
            android:textSize="32sp"
            android:textStyle="bold"
            android:layout_marginBottom="16dp" />

        <!-- Subtitle -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Please register this device on the platform"
            android:textColor="#a0a0a0"
            android:textSize="18sp"
            android:layout_marginBottom="40dp" />

        <!-- Device ID Card -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/enrollment_card_bg"
            android:padding="24dp"
            android:gravity="center">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="DEVICE ID"
                android:textColor="#888888"
                android:textSize="14sp"
                android:letterSpacing="0.2"
                android:layout_marginBottom="8dp" />

            <TextView
                android:id="@+id/deviceIdText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Loading..."
                android:textColor="#4fc3f7"
                android:textSize="28sp"
                android:textStyle="bold"
                android:fontFamily="monospace"
                android:layout_marginBottom="16dp" />

            <Button
                android:id="@+id/copyButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="📋  Copy to Clipboard"
                android:textColor="#ffffff"
                android:textSize="14sp"
                android:textAllCaps="false"
                android:background="@drawable/copy_button_bg"
                android:paddingHorizontal="24dp"
                android:paddingVertical="12dp" />

        </LinearLayout>

        <!-- Instructions -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Use this ID to add the device in the admin dashboard"
            android:textColor="#666666"
            android:textSize="14sp"
            android:layout_marginTop="32dp"
            android:gravity="center" />

        <!-- Retry info -->
        <TextView
            android:id="@+id/retryText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Checking enrollment status..."
            android:textColor="#555555"
            android:textSize="12sp"
            android:layout_marginTop="24dp" />

    </LinearLayout>

</FrameLayout>